import styles from './History.module.scss';

export function History() {
    return (
        <main className={styles.historyContainer}>
            <h1>History</h1>
            <p>This page will display the user's quiz history.</p>
            {/* Future implementation will go here */}
        </main>
    );
}